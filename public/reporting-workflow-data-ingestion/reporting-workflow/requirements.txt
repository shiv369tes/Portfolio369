pandas>=2.0
openpyxl>=3.1        # Excel (.xlsx) support
sqlalchemy>=2.0      # optional: only needed for database sources
PyYAML>=6.0           # optional: only needed if loading config.yaml directly

# Add a DB driver matching your database, e.g.:
# psycopg2-binary     # PostgreSQL
# pymysql             # MySQL
